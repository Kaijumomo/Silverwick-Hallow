# Phase 9B handoff — Traveler support

**Late-arrival correction (2026-09-09):** The independent final review found that generic active-game additions could still create ordinary players. This is corrected at the arrival command boundary: `addPlayer`, `addPlayerToSeat`, `addEmptySeat`, and `assignPendingToSeat` default new Day/Night arrivals to Travelers. Filling an old ordinary empty seat clears its planned identity and starts Traveler onboarding. Setup additions remain ordinary unless explicitly marked Traveler. Occupied seats and reconnects retain their identity. The seating popup cannot overwrite the active default with an unchecked setup value, including when play begins while the popup is open. Active add controls identify Traveler arrival; online addition still uses approved Firebase-first seating. No authority, setup-count, projection, canonical-data, or night-order architecture was changed by this correction.

Correction verification: **121 focused tests passed** (80 Traveler/arrival tests plus 41 setup-command regressions); **643 normal tests passed**; **58 emulator tests passed**; all have **0 failures and 0 skips**. Typecheck, `test:full`, production build, and diff check passed. A temporary smoke fixture initially stopped `test:full` on an unused import; the fixture was corrected and removed before the final passing run. Rendered checks exercised setup ordinary addition, explicit pregame Traveler, and generic Night/Day arrivals with onboarding and unchanged planned/ordinary count of five. Layouts were inspected at 390×844, 820×1180, and 1440×900. The in-app browser does not support native `prompt()`, so the isolated fixture supplied a test name; the actual add controls, store commands, drawer, and phase controls were used. Existing ordinary-player reconnect was verified through the real handshake and writer with the memory backend. No temporary fixture source remains. The correction adds `src/features/grimoire/arrivalDefaults.test.tsx`; the workspace now has 20 modified tracked files and 7 added files, all uncommitted. **READY FOR PHASE 9B RE-REVIEW.** The original implementation handoff below records the preceding verification pass.

1. **Starting state.** The supplied workspace initially contained an empty Git repository on `main`, with no commits, remote, or source files. GitHub `Kaijumomo/Silverwick-Hallow` was accessible and its `main` was exactly `7eafe768edb6ada4b8657fdfb86922c126f3849c`. The empty workspace was populated from that remote. Before source edits, `main` tracked `origin/main`, both pointed to that baseline, the working tree was clean, and `git diff --check` passed. No commit or push was made.

2. **Architecture confirmation.** Targeted inspection confirmed the approved assumptions: canonical Traveler definitions, separate setup population, explicit shown identity, allowlisted projections, ordered private delivery, Firebase-first membership, and private JSON checkpoints. No material architecture contradiction was found. One small projection adjustment was necessary: a Traveler's player-view identity may omit `shownAlignment`, because defaulting it to good would invent information. Ordinary identity derivation and incomplete-snapshot handling remain intact. The full architecture audit was not repeated.

3. **Current state model.** `actualRole` holds the Traveler character. Optional `actualAlignment` is current Storyteller truth, changed only through Storyteller controls; it is never inferred from `shownAlignment`. Optional `travelerArrival` holds Demon-information completion, personal first-night completion, the completion night needed to prevent a duplicate wake that same night, and a narrow public-starting-information check where needed. `alive` remains existing current life state; optional `exiled` records the current exile result. Departure uses membership/seat absence, without a new history record. Disconnect does not mutate these facts.

4. **Arrival workflow.** Offline, Add Traveler creates a physical seat and opens the drawer. Online, it creates an empty Traveler seat and opens the existing approved membership flow; the Storyteller accepts a waiting player before editing identity. Existing empty-seat assignment also offers “Arriving as a Traveler.” Select the public character and privately choose Good/Evil at the top of the drawer. Neither arrival nor departure changes `plannedPlayerCount` or reopens Setup. Good Travelers receive only relevant procedure checks. Evil Travelers get a Demon-information prompt, with explicit digital preparation/delivery or an in-person completion action. Gnome receives a canonical public-starting-information check; no player is selected or announced automatically.

5. **Projections.** Storyteller state includes truth alignment, arrival state, exile, drafts, and delivery metadata. Public state and other players see the canonical Traveler character plus existing public seating, life, and presence fields. They never receive actual alignment or arrival/exile administration fields. The Traveler sees their public character, canonical role/Almanac information, explicitly shown alignment if the Storyteller chooses to show it, and only privately delivered information. Demon delivery contains a player's id/name/seat, not their character, Minions, or bluffs. Ordinary secret roles are not newly published.

6. **Death, exile, departure.** Marking dead retains the player and membership. Exile sets `exiled: true` and `alive: false`, retaining membership and private access; restoring alive clears current exile. Departure uses the existing remote revocation command before local removal, removes public/session state, and cannot silently reconnect to the old private seat. The established user-facing removal outcome remains `revoked`. No digital exile voting is required or implemented.

7. **Night Assistant.** On game Night 1, Traveler procedures retain their canonical positions. On later nights, a pending personal first-night procedure appears as a separate arrival check using canonical text. Its position is explicitly a Storyteller timing decision, not an invented merged canonical order. Completion persists, suppresses a second ordinary wake that night, and leaves the global day/night unchanged. Character reassignment clears relevant current-night progress; explicit progress reset and undo stay compatible. Apprentice gained abilities remain manual checks.

8. **Persistence.** Storyteller persistence version is **11**, formerly 10. The schemas and private checkpoint decoder accept new optional fields. Migration repairs a canonical Traveler's public character in current and undo snapshots but does not invent alignment, arrival timing, completion, or exile. Running legacy Travelers receive targeted unknown-completion guidance. Player-session persistence needs no new historical migration. New records omit absent optional fields rather than serializing `undefined`; an existing removal cleanup that could emit `privateInfo: undefined` was also corrected. Tests cover rehydration and authoritative writer takeover over stale local Traveler state.

9. **UI and privacy.** The drawer starts with the compact Traveler controls, then relevant next actions. Death, Exile Traveler, and Traveler leaves game remain distinct. Actual alignment selection does not itself reveal alignment; “Show alignment to Traveler” is explicit. Privacy Mode removes secret controls and information from the DOM/accessibility tree while preserving public Traveler character. The player role card, Town character label, public display, and Almanac reuse canonical data. Rendered inspection led to an opaque phone Night Assistant background so underlying tokens do not interfere with reading.

10. **Final verification.** All final gates passed:

    | Gate | Passed | Failed | Skipped |
    | --- | ---: | ---: | ---: |
    | Focused Phase 9B, three test files | 67 | 0 | 0 |
    | Normal `npm test` | 630 | 0 | 0 |
    | `npm run test:rules:run`, local Firebase emulator | 58 | 0 | 0 |
    | `npm run test:full` | 630 normal + 58 emulator | 0 | 0 |

    `npm run typecheck`, production `npm run build`, and `git diff --check` passed. The full gate retains Phase 7 night-order, Phase 8 responsive/accessibility, Phase 9 setup, identity, private-information, and multiplayer regressions. Emulator execution used the installed Java 17 runtime and local demo project; Firebase rules were not weakened or modified. Development failures were corrected before the final gate.

11. **Rendered review.** Chromium checked **390×844**, **820×1180**, and **1440×900**, with zero page errors. At each size: pregame addition, active-game addition, Good/Evil selection, public role, explicit alignment display, Demon preparation/send, personal arrival completion, death, exile, departure, reconnect, Privacy Mode, drawer and Night Assistant. A further online waiting-player assignment and Gnome check were exercised at each size. Screenshots of the actual components were inspected, including public display, role card, and Almanac. Browser networking used the existing memory backend with the real writer/handshake code; Firebase authorization was separately exercised by the emulator suite. This was not a production-device multiplayer session. Temporary fixture source files were removed.

12. **Boundaries and independent review.** No nominations/voting system, Phase 10 history, event sourcing, full Traveler ability automation, or custom-script redesign was added. The Storyteller remains final authority. The separate read-only reviewer found three implementation issues—delivery readiness after identity changes, stale arrival progress on character changes, and Gnome starting guidance. All were fixed and regression-tested; the reviewer confirmed no remaining concrete findings. Traveler rule reference: [official Traveler rules](https://wiki.bloodontheclocktower.com/Travellers).

13. **Remaining work.** **Blocking:** none known. **Non-blocking:** ambiguous Demon contexts, custom/unverified roles, modifiers, and late timing require the intended manual Storyteller check; legacy unknown state needs review. **Phase 10:** historical/temporal rules remain deferred. **Phase 11:** no work undertaken and no new prerequisite identified. **Future visual/UX:** optional live-table feedback on wording and density; no known unusable viewport in the rendered review.

14. **Final Git state.** HEAD remains `7eafe768edb6ada4b8657fdfb86922c126f3849c`, branch `main`, synchronized with `origin/main`. All Phase 9B changes are uncommitted and unstaged. Twenty existing source/test/style files are modified; six files are added including this handoff. No unexpected files remain. Tracked build metadata was restored; temporary browser fixtures, emulator logs, and generated build output were removed.

    Modified: `src/features/grimoire/GrimoireCircle.tsx`, `SeatAssignPopup.tsx`; `src/features/nightOrder/NightOrderPanel.tsx`, `nightOrder.ts`; `src/features/player/PlayerScreen.tsx`, `PrivateInformation.tsx`; `src/features/players/PlayerDrawer.tsx`, `PlayerInformation.tsx`; `src/features/setup/setupAnalyzer.ts`, `setupCommands.test.ts`; `src/firebase/privatePacketCommands.ts`, `rules.spec.ts`, `snapshots.ts`; `src/stores/privatePackets.ts`, `projections.test.ts`, `projections.ts`, `schemas.ts`, `storytellerStore.ts`, `types.ts`; `src/styles/components.css`.

    Added: `PHASE9B.md`; `src/features/players/TravelerArrival.tsx`, `TravelerArrival.test.tsx`; `src/firebase/travelers.test.ts`; `src/stores/travelers.ts`, `travelers.test.ts`.

15. **Verdict:** READY FOR PHASE 9B HUMAN REVIEW
